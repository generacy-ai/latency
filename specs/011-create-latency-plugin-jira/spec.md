# Feature Specification: Create latency-plugin-jira + interface

**Branch**: `011-create-latency-plugin-jira` | **Date**: 2026-02-03 | **Status**: Draft

## Summary

## Parent Epic
Part of #1 (Phase 1 - Repository & Package Setup) - Phase 1C

## Description

Create the Jira implementation with two packages:
1. **latency-plugin-jira** - The implementation
2. **jira-interface** - Types for consumers

## Plugin Package

```typescript
// @generacy-ai/latency-plugin-jira

import { AbstractIssueTrackerPlugin } from '@generacy-ai/latency-plugin-issue-tracker';
import { JiraIssue, JiraIssueSpec, JiraTransition } from '@generacy-ai/jira-interface';

export class JiraPlugin extends AbstractIssueTrackerPlugin {
  // Jira-specific implementation
  // Uses Jira REST API
  
  // Jira-specific methods
  async transitionIssue(issueId: string, transition: JiraTransition): Promise<JiraIssue>;
  async getTransitions(issueId: string): Promise<JiraTransition[]>;
  async linkIssues(fromId: string, toId: string, linkType: string): Promise<void>;
}
```

## Interface Package

```typescript
// @generacy-ai/jira-interface

import { Issue } from '@generacy-ai/latency';

export interface JiraIssue extends Issue {
  key: string;              // e.g., "PROJ-123"
  projectKey: string;
  issueType: JiraIssueType;
  priority: JiraPriority;
  status: JiraStatus;
  sprint?: JiraSprint;
  storyPoints?: number;
  epicLink?: string;
  customFields: Record<string, unknown>;
}

export function isJiraIssue(issue: Issue): issue is JiraIssue;
export function getJiraIssueUrl(issue: JiraIssue, baseUrl: string): string;
```

## Tasks

- [ ] Create both package directory structures
- [ ] Implement JiraPlugin extending abstract
- [ ] Define Jira-specific types in interface package
- [ ] Implement type guards and helpers
- [ ] Write tests

## Dependencies

### Plugin
- `@generacy-ai/latency-plugin-issue-tracker`
- `@generacy-ai/jira-interface`
- Jira API client

### Interface
- `@generacy-ai/latency` (types only)

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
