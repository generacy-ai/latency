# Feature Specification: Remove legacy .claude/autodev.json

**Branch**: `046-summary-delete-legacy-claude` | **Date**: 2026-03-13 | **Status**: Draft

## Summary

Delete the legacy `.claude/autodev.json` configuration file. This file has been superseded by `.generacy/config.yaml` as part of the broader migration effort tracked in generacy-ai/generacy#373.

The file contains only default configuration (`paths.specs: "specs"`) that is now handled by the new config system.

## User Stories

### US1: Clean up legacy configuration

**As a** developer working in the latency repository,
**I want** the deprecated `.claude/autodev.json` file removed,
**So that** there is a single source of truth for project configuration and no confusion about which config file is authoritative.

**Acceptance Criteria**:
- [ ] `.claude/autodev.json` is deleted from the repository
- [ ] No remaining references to `.claude/autodev.json` in project files
- [ ] The `.claude/` directory is preserved (it contains other valid files like `CLAUDE.md` settings)

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | Delete `.claude/autodev.json` | P1 | File contains only default config now handled elsewhere |
| FR-002 | Verify no code depends on `.claude/autodev.json` | P1 | Ensure nothing reads this file at runtime or build time |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | File removed | `.claude/autodev.json` no longer exists in repo | `git status` shows deletion |
| SC-002 | No breakage | Build and tests pass after removal | CI pipeline green |

## Assumptions

- The `.generacy/config.yaml` (or equivalent) in the generacy tooling handles all configuration previously in `autodev.json`
- No runtime code in this repository reads `.claude/autodev.json`

## Out of Scope

- Migration of configuration values (already handled by generacy-ai/generacy#373)
- Changes to `.generacy/config.yaml` or any other config files
- Removal of other files in the `.claude/` directory

---

*Generated by speckit*
