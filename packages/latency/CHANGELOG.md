# @generacy-ai/latency

## 0.1.2

### Patch Changes

- c4a4077: Publish to the `stable` npm dist-tag. No code changes — this changeset exists to force a version+publish cycle so a `stable` dist-tag is created on npm (the orchestrator entrypoint installs `@generacy-ai/*@stable`, but `stable` was never populated for any package — only `latest`).

## 0.1.1

### Patch Changes

- d90d548: Seed preview dist-tag for downstream dependency resolution
